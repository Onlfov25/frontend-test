import React from 'react'
import { Form, Input, Button } from 'antd'
export default () => {
    return (
        <div>
            <Form>
                <Input />>
                <Input />
                <Button />
            </Form>    
        </div>
    )
}